# API key clients must send in x-api-key header
API_SECRET_KEY=your-secret-api-key-change-in-production

# LLM: per-provider arrays (comma-separated). Add as many keys as you have; each is tried in order.
# Try order: Groq keys → OpenRouter keys → GitHub keys → OpenAI → etc. On failure, next key is tried.
# Example: GROQ_API_KEYS=gsk_xxx,gsk_yyy  or  OPENROUTER_API_KEYS=sk-or-xxx,sk-or-yyy
GROQ_API_KEYS=gsk_...
OPENROUTER_API_KEYS=sk-or-...
GITHUB_API_KEYS=ghp_...
OPENAI_API_KEYS=sk-...
XAI_API_KEYS=xai-...
DEEPSEEK_API_KEYS=sk-...

# Local Ollama (no API key). Set for local testing; tried first when set.
# OLLAMA_BASE_URL=http://localhost:11434/v1

# Optional: LLM_MODEL=, LLM_BASE_URL=

# Optional: path to file with extra scam keywords (one per line). Used by scam_detection.
# SCAM_KEYWORDS_FILE=config/scam_keywords.txt

# GUVI callback (mandatory for evaluation)
GUVI_CALLBACK_URL=https://hackathon.guvi.in/api/updateHoneyPotFinalResult

# Logging (production-grade)
# LOG_LEVEL=INFO          # DEBUG, INFO, WARNING, ERROR
# LOG_FORMAT=json         # json (structured for aggregators) or console (human-readable)
# LOG_FILE=logs/app.log  # optional; daily rotation, 30 backups
